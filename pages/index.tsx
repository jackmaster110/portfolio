import Header from "components/Header";
import type { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <div className="page-container">
            <Header />
        </div>
    );
};

export default Home;
